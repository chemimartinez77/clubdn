# 📋 Tareas Pendientes - Club DN

**Fecha:** 14 de Diciembre de 2025 - 02:29 AM
**Última actualización:** Implementación de Perfiles de Usuario completada

---

## ✅ Completado (4 de 5 prioridades altas)

### 1. Sistema de Notificaciones/Toasts ✅
- Toast component con animaciones
- ToastContext con gestión de estado
- Hook useToast
- Integrado en Login, Register, PendingApprovals

### 2. Dashboard de Usuario ✅
- WelcomeCard con saludo personalizado
- StatsCard con estadísticas (preparadas para eventos/juegos)
- QuickActionsCard con accesos rápidos
- Home.tsx actualizado como dashboard completo

### 3. Dashboard de Admin ✅
- Backend: statsController, statsRoutes (/api/stats/admin)
- Frontend: AdminDashboard con métricas en tiempo real
- Estadísticas: usuarios, logins, crecimiento
- Visualización de usuarios recientes por estado

### 4. Perfiles de Usuario Extendidos ✅
- **Backend:**
  - Modelo UserProfile en Prisma (migración creada)
  - profileController con CRUD completo
  - profileRoutes (/api/profile/me, /api/profile/:userId)
  - Registrado en server/src/index.ts
- **Frontend:**
  - Tipos en types/profile.ts
  - Profile.tsx con vista y edición completa
  - Ruta /profile agregada en App.tsx
  - Enlace habilitado en QuickActionsCard
- **Características:**
  - Información personal: teléfono, fecha nacimiento, bio
  - Preferencias de juego: favoritos, estilo
  - Redes sociales: Discord, Telegram
  - Configuración: notificaciones, emails

---

## 🔴 Pendiente (1 de 5 prioridades altas)

### 5. Sistema de Eventos (Fase 5.1) - ALTA PRIORIDAD

**Backend requerido:**
```prisma
// Modelos necesarios en schema.prisma

enum EventStatus {
  SCHEDULED
  ONGOING
  COMPLETED
  CANCELLED
}

enum RegistrationStatus {
  CONFIRMED
  CANCELLED
  WAITLIST
}

model Event {
  id              String   @id @default(cuid())
  title           String
  description     String   @db.Text

  date            DateTime
  location        String
  address         String?

  maxAttendees    Int
  registrations   EventRegistration[]

  status          EventStatus @default(SCHEDULED)

  createdBy       String
  organizer       User     @relation("EventOrganizer", fields: [createdBy], references: [id])

  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
}

model EventRegistration {
  id          String   @id @default(cuid())
  eventId     String
  event       Event    @relation(fields: [eventId], references: [id], onDelete: Cascade)
  userId      String
  user        User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  status      RegistrationStatus @default(CONFIRMED)

  createdAt   DateTime @default(now())

  @@unique([eventId, userId])
}
```

**Controladores a crear:**
- `server/src/controllers/eventController.ts`
  - getEvents() - Listar todos los eventos
  - getEvent() - Ver detalles de un evento
  - createEvent() - Crear evento (admin)
  - updateEvent() - Actualizar evento (admin/organizador)
  - deleteEvent() - Cancelar evento (admin)
  - registerToEvent() - Inscribirse a evento
  - unregisterFromEvent() - Cancelar inscripción
  - getEventAttendees() - Ver asistentes

**Rutas a crear:**
- `server/src/routes/eventRoutes.ts`
  - GET /api/events
  - GET /api/events/:id
  - POST /api/events (admin)
  - PUT /api/events/:id (admin)
  - DELETE /api/events/:id (admin)
  - POST /api/events/:id/register
  - DELETE /api/events/:id/register

**Frontend a crear:**
- `client/src/types/event.ts` - Tipos
- `client/src/pages/Events.tsx` - Lista de eventos con calendario
- `client/src/pages/EventDetail.tsx` - Detalle y registro
- `client/src/pages/admin/EventManagement.tsx` - CRUD para admins
- `client/src/components/events/EventCard.tsx`
- `client/src/components/events/EventCalendar.tsx`

**Características:**
- Calendario mensual de eventos
- Sistema de inscripción/desinscripción
- Lista de espera cuando se llena
- Vista de asistentes
- Filtros por fecha, estado
- Notificaciones de eventos próximos

---

## 🟡 Funcionalidades POST_MVP Pendientes (Media Prioridad)

### 6. Gestión de Juegos (Fase 4)
- Biblioteca de juegos del club
- Sistema de préstamos
- Reseñas y ratings
- Colección personal de usuarios

### 7. Dark Mode (Fase 1.2)
- ThemeContext con toggle
- Variables CSS para tema oscuro
- Persistencia en localStorage

### 8. Chat en Tiempo Real (Fase 6.1)
- Socket.io para WebSockets
- Mensajes directos
- Canales de eventos

### 9. Rate Limiting (Fase 7.1)
- express-rate-limit
- Límites por endpoint

### 10. Seguridad Adicional (Fase 7.2)
- Helmet.js
- CSRF protection
- Input sanitization

---

## 🟢 Mejoras Técnicas Sugeridas (Baja Prioridad)

### Frontend
- Code splitting
- Lazy loading de rutas
- Skeleton loaders en lugar de spinners
- Framer Motion para animaciones
- Virtual scrolling para listas largas

### Backend
- Paginación en todas las listas
- Caché con Redis
- Winston para logging estructurado
- Query optimization

### Testing
- Vitest para componentes
- Jest para backend
- Tests E2E con Playwright/Cypress

### Deployment
- Docker containerization
- CI/CD con GitHub Actions
- Hosting configurado

---

## 📝 Notas Técnicas

### Problemas Conocidos
1. **Prisma Client EPERM**: Al regenerar el cliente con el servidor corriendo, usar:
   ```bash
   rm -rf node_modules/.prisma/client && npx prisma generate
   ```

2. **TypeScript en authController.ts**: Error de tipo JWT en línea 308 (pre-existente, no afecta nueva funcionalidad)

### Base de Datos Actual
- PostgreSQL en Neon
- Modelos: User, UserProfile, EmailLog, LoginAttempt
- Próximo: Event, EventRegistration

### Stack Tecnológico
- **Frontend**: React + TypeScript + Vite + TailwindCSS + TanStack Query
- **Backend**: Node.js + Express + TypeScript + Prisma
- **Database**: PostgreSQL (Neon)
- **Auth**: JWT

---

## 🎯 Próximos Pasos Recomendados

1. **INMEDIATO**: Implementar Sistema de Eventos (Fase 5.1)
   - Es la funcionalidad core que más valor aporta
   - Permitirá organizar las sesiones del club
   - Aproximadamente 1-2 días de desarrollo

2. **CORTO PLAZO**: Gestión de Juegos (Fase 4)
   - Complementa perfectamente el sistema de eventos
   - Permite llevar control de la biblioteca del club

3. **MEDIO PLAZO**: Chat y Notificaciones (Fase 6)
   - Mejora la comunicación entre miembros
   - Recordatorios automáticos de eventos

4. **LARGO PLAZO**: Optimizaciones y Testing
   - Una vez tengamos las funcionalidades core

---

**Generado automáticamente:** 2025-12-14 02:29 AM
